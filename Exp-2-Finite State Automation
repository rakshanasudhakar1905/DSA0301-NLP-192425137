s = input("Enter a string: ")

state = 0

for ch in s:
    if state == 0:
        state = 1 if ch == 'a' else 0
    elif state == 1:
        state = 2 if ch == 'b' else 1 if ch == 'a' else 0
    else:
        state = 1 if ch == 'a' else 0

if state == 2:
    print("Accepted")
else:
    print("Rejected")
